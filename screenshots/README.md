# Screenshots

`public/` holds the redacted, publication-safe captures referenced by the top-level
README. Every image here has internal addressing masked and all third-party data
(leaked keys, exposed-host IPs and geolocation) removed.

Do not commit unredacted originals. The `apikey-monitor` capture is intentionally
cropped to its summary panel only — the list of third-party vulnerable repositories
is never published.
